database:
  host: 127.0.0.1
  user: root
  password: "12345"
  name: exam_monitor_db

server:
  host: 0.0.0.0
  port: 8000

paths:
  evidence_dir: server_evidence
  database_dir: server_database
  submissions_dir: server_submissions
